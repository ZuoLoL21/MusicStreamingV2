# =============================================================================
# REQUIRED - Secrets, Connections, and Service URLs
# =============================================================================
USER_CRUD_CONNECTION_STRING="postgresql://postgres:postgres@localhost:5432/music_streaming"
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin123
EVENT_INGESTION_SERVICE_URL=http://localhost:8005
VAULT_ADDR=http://localhost:8200
VAULT_TOKEN="root"

# =============================================================================
# OPTIONAL - Override defaults if needed
# =============================================================================
# MINIO_BUCKET_NAME=music-streaming                   # Default: music-streaming
# JWT_STORE_PATH=jwt/backend                          # Default: jwt/backend
# VAULT_APPLICATION_NAME=service_user_database        # Default: service_user_database

# JWT Expiration Times (defaults from libs/consts/jwt_and_ctx.go)
# JWT_TIME_IN_M_NORMAL=10                             # Default: 10 minutes
# JWT_TIME_IN_D_REFRESH=10                            # Default: 10 days
# JWT_TIME_IN_M_SERVICE=2                             # Default: 2 minutes
# VAULT_JWT_TIMEOUT_SECONDS=30                        # Default: 30 seconds
